package com.lcb.one.ui.theme

import androidx.compose.ui.graphics.Color

private val seedBlue = Color(0XFF206CFF)
private val seedOrange = Color(0XFFFF7024)
private val seedGreen = Color(0xFF35C44F)
private val seedCyan = Color(0xFF02BEBF)
private val seedPurple = Color(0xFF7434DC)

val defaultSeeds by lazy {
    listOf(
        seedBlue,
        seedOrange,
        seedGreen,
        seedCyan,
        seedPurple,
    )
}